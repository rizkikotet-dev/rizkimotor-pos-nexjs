{
  "framework": "nextjs",
  "buildCommand": "node prisma/prepare.js && npx prisma generate --schema=prisma/schema.prepared.prisma && npx prisma db push --schema=prisma/schema.prepared.prisma --skip-generate --accept-data-loss && next build",
  "installCommand": "npm install",
  "outputDirectory": ".next",
  "git": {
    "deploymentEnabled": {
      "main": true,
      "develop": true
    }
  }
}
